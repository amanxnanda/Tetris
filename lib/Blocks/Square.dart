import 'package:flutter/material.dart';

class SquareBlock extends StatelessWidget {
  final int endPost;
  const SquareBlock({Key key, this.endPost}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
     
    );
  }
}
